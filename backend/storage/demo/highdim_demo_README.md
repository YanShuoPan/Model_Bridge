# 高維度示範數據說明

## 📊 數據概況

- **檔案名稱**: `highdim_demo.csv`
- **樣本數**: 100
- **變數數**: 50 個預測變數 (X1-X50) + 1 個結果變數 (outcome)
- **數據類型**: 高維度連續型數據 (p >> n)

## 🎯 適用方法

此數據集專為 **OGA-HDIC (高維度變數選擇)** 方法設計，適合展示：
- 高維度變數選擇
- 稀疏模型估計
- 特徵篩選

## 📋 變數說明

### 預測變數
- **X1 - X50**: 50 個連續型預測變數
  - 所有變數均來自標準常態分佈 N(0, 1)
  - **真正重要的變數**: 只有前 5 個 (X1-X5)
  - **不重要的變數**: X6-X50 (干擾變數)

### 結果變數
- **outcome**: 連續型結果變數
  - 生成方式: `outcome = 3.5*X1 - 2.8*X2 + 1.9*X3 - 1.5*X4 + 2.2*X5 + noise`
  - 誤差項: noise ~ N(0, 4)

## 🔍 真實模型

真實的資料生成模型為：

```
outcome = 3.5*X1 - 2.8*X2 + 1.9*X3 - 1.5*X4 + 2.2*X5 + ε
```

其中：
- 係數 = [3.5, -2.8, 1.9, -1.5, 2.2, 0, 0, ..., 0]
- 只有 5 個非零係數（稀疏性）
- ε ~ N(0, 4) 為隨機誤差

## 🎓 預期學習成果

使用 OGA-HDIC 分析此數據，你應該能夠：

1. **變數選擇**: 正確識別出 X1-X5 為重要變數
2. **係數估計**: 估計的係數應接近真實值 [3.5, -2.8, 1.9, -1.5, 2.2]
3. **HDIC 曲線**: 觀察 HDIC 在選擇 5-7 個變數時達到最小值
4. **預測表現**: R² 應在 0.6-0.8 之間（取決於隨機誤差）

## 💡 使用建議

### 基本分析
```python
# 讀取數據
import pandas as pd
df = pd.read_csv('highdim_demo.csv')

# 設定角色
roles = {"y": "outcome"}

# 執行 OGA-HDIC
params = {
    "Kn": 20,           # 最多選擇 20 個變數
    "c1": 5,            # HDIC 懲罰參數
    "HDIC_Type": "HDBIC",  # 使用 HDBIC
    "c2": 2,
    "c3": 2.01
}
```

### 預期結果
- **選擇的變數數量**: 5-10 個
- **核心變數**: X1, X2, X3, X4, X5 應該被選中
- **R² (After Trimming)**: 約 0.65-0.75
- **HDIC 最佳化點**: k ≈ 5-7

## ⚠️ 注意事項

1. **高維度特性**: 此數據 p/n = 0.5，屬於中度高維度情境
2. **稀疏性**: 真實模型只有 5/50 = 10% 的變數有效
3. **共線性**: 變數間相互獨立，無多重共線性問題
4. **隨機性**: 每次生成數據時誤差項不同，結果會有些微差異

## 📚 相關文獻

高維度變數選擇是統計學習的重要課題，特別適用於：
- 基因體學研究 (p >> n)
- 經濟計量模型
- 機器學習特徵選擇
- 訊號處理

---

*此數據集由 AI Agent Statistical Platform 自動生成，用於教學與展示目的。*
